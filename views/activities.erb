<div id="wrapper">
    <!-- Navigation -->
    <div id="page-wrapper">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">Activities</h1>
            </div>
        </div>
            <% activities.each do |activity| %>
        	    <div class="list-group">
        	      <a href="/activities/<%= activity['slug'] %>" class="list-group-item"><%= activity['name'] %></a>
            <% end %>
        	    </div>
    </div>
    <!-- /#page-wrapper -->

</div>
<!-- /#wrapper -->